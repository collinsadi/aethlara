services:
  api:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: aethlara-api
    restart: unless-stopped
    env_file:
      - .env
    environment:
      PORT: "${PORT:-8081}"
    ports:
      - "${PORT:-8080}:${PORT:-8081}"
